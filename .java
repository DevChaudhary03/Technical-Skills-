// # given an array of integar no. calculate the pivot index of this array . the pivot index is the index where the 
// # rhe sum of all the numbers strictly to the left of the index equals to the sum of number strictly to the index of right .
// # if the index on the left edge of the array then the lef t sum os zero because there are no elments to the left , this also apply to the right edge of the array 
// # written the left most pivot index , if no such index exists written - 1.

